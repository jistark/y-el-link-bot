TELEGRAM_BOT_TOKEN=
TELEGRAPH_ACCESS_TOKEN=
POLLER_CHAT_ID=
PROXY_URL=                # IPRoyal Web Unblocker — http://USER:PASS@unblocker.iproyal.com:12323
                          # Password may include dashboard params like _country-us (auto-stripped per request)
PROXY_DOMAINS=            # Optional CSV override of default proxy-required domains (ft.com,bloomberg.com,...)
PROXY_JS_DOMAINS=         # Optional CSV of domains needing JS rendering (default: dolar.cl)
FETCH_SHARED_SECRET=      # Bearer token for the POST /fetch HTTP endpoint (external IPRoyal proxy access).
                          # Generate with: openssl rand -hex 32  — share with calling services.
